// theme.ts
import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  // Customize your theme here
  colors: {
    blue: {
      700: "#2B6CB0", // Example customization
    },
    green: {
      500: "#38A169", // Example customization
    },
  },
});

export default theme;
